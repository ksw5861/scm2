############################
# Vue (프론트엔드)
############################

# 의존성 패키지 → 설치 시 자동 생성, Git에 불필요
/node_modules/

# 빌드 결과물 → 배포 시 생성되는 산출물, Git에 포함 불필요
/dist/

# 패키지 매니저 로그 파일 → 개인 환경에 따라 다르므로 공유 불필요
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/pnpm-debug.log*

# IDE 설정 파일 → 개인별 환경차로 충돌 발생 가능
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 환경 변수 파일 → DB 비밀번호, API Key 등 민감정보 보안상 제외
.env
.env.local
.env.*.local


############################
# Spring Boot (백엔드)
############################

# Gradle 캐시 및 빌드 결과물 → 자동 생성되므로 Git에 불필요
.gradle/
/build/
/out/

# Maven 빌드 결과물 → target 폴더는 빌드시 자동 생성
target/

# IDE 설정 파일 → 개인 개발환경별로 다름, 충돌 방지 목적
*.iml
.idea/
.vscode/

# 로그 파일 → 실행 시점에 따라 달라지므로 Git에 포함 불필요
*.log
logs/

# 임시 및 백업 파일 → 불필요한 로컬 개발 산출물
*.tmp
*.bak
*.swp

# 운영체제 자동 생성 파일
.DS_Store        # macOS
Thumbs.db        # Windows

# 환경설정 파일 → 민감정보(DB, API Key) 포함 가능
application-*.yml
application-*.properties


############################
# 공통적으로 제외하면 좋은 항목
############################

# 백업 / 임시 파일
*.bak
*.tmp
*.swp
